// import config from "./config";
import app from "./app";

export default app;

// const port = config.port || 5000;

// app.listen(port, () => {
//   console.log(`example app listening on port ${port}`);
// })